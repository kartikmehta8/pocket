node_modules
dist
.next
.turbo
*.tsbuildinfo
# Every .env variant is a secret unless it is explicitly an example.
.env
.env.*
!.env.example
!.env.*.example
.logs
.DS_Store
coverage

# subgraph
subgraph/generated
subgraph/build
subgraph/node_modules

# Working notes and retired scaffolding. Kept on disk for reference, never pushed.
temp

# Agent skills, vendored per machine rather than shipped with the repository.
.claude
