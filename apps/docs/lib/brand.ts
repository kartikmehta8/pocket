/**
 * How Purse describes itself.
 *
 * @remarks Must match `apps/dashboard/lib/brand.ts`, which carries the same
 * note pointing back here. The product describes itself the same way in every
 * place a reader might meet it.
 */

/** What it does, in one sentence. Used for the site's meta description. */
export const DESCRIPTION = 'Give each agent its own wallet and a hard cap it cannot raise.';
